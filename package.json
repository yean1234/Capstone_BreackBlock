{
  "name": "brick-breaker",
  "version": "0.1.0",
  "private": true,
  "description": "HTML5 brick breaker game (vanilla JS).",
  "scripts": {
    "dev": "live-server public --port=5173 --no-browser",
    "start": "serve public -l 3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.4"
  }
}
